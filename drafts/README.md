# Work in progress (not part of the live site)

This folder lives only on the `claude/story-rewrite-drafts` branch. It is never merged into `main`.

- `story-style-review.md`: review of all stories against the studio writing rules and the health copy rule, with the rewrite priority list.
- `pristine-test-report-2026-09-24.md`: full site and studio test report.
- `rewrites/teeth.json` + `teeth-review.md`: teeth article rewrite awaiting approval. Next: magnesium, 10 proven ways, then priority 2 and 3. Fermented foods stays as is (owner decision). Turmeric line in chickpea curry stays (owner decision).
- `tools/`: scripts used to re-publish stories through the real studio in a sealed browser (extract content, clear grids, run studio publish). They need the studio simulation mocks from the earlier session to run again.

Open PRs to merge in order: #1, #2, #3, #4, then #5.
